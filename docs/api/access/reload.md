---
title: access.reload
description: "Перечитывает `groups.lua` и `users.lua` с диска"
---

# access.reload

Перечитывает `groups.lua` и `users.lua` с диска.

```lua
access.reload()
```

## Возвращает

Ничего.

Без полного `lua_reload`.
