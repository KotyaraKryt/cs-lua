---
title: hook — Жизнь сервера
description: "Одна система на движковые события и на свои"
---

# Жизнь сервера

## map_change {#map_change}

Карта заканчивается.

```lua
hook.add("map_change", id, function(e)
	...
end)
```

### Поля события {#map_change-поля события}

| поле | тип |  |
|---|---|---|
| `e.map` | string | имя карты, которая заканчивается |

Lua-состояние живёт дальше. Место, где пишут данные на диск и снимают эффекты с мира.

## plugin_unload {#plugin_unload}

Плагин или всё состояние уходит.

```lua
hook.add("plugin_unload", id, function(e)
	...
end)
```

### Поля события {#plugin_unload-поля события}

| поле | тип |  |
|---|---|---|
| `e.plugin` | string \| nil | id уходящего плагина; `nil` — гасится всё состояние |

Подписываться стоит, если держишь ссылки на чужой плагин. Для снятия собственных эффектов есть [`plugin.on_unload`](../plugin/index.md#on_unload) — он про твой плагин.
