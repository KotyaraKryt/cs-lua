---
title: sv — Сервер
description: "Сервер: часы, карта, консоль, переменные движка"
---

# Сервер

## sv.time {#time}

Серверные часы в секундах.

```lua
sv.time()
```

### Возвращает

| тип |  |
|---|---|
| `number` | секунды с дробной частью |

Те же часы, на которых идут [таймеры](../timer/index.md). Обнуляются при смене карты.

<Note>
`os.clock()` — это CPU-время, оно сильно расходится на сервере,
который спит между кадрами. Для интервалов бери `sv.time()`.
</Note>

## sv.map {#map}

Имя текущей карты.

```lua
sv.map()
```

### Возвращает

| тип |  |
|---|---|
| `string` | `"de_dust2"` |

## sv.cmd {#cmd}

Ставит команду в очередь серверной консоли.

```lua
sv.cmd(fmt, ...)
```

### Аргументы

| # | имя | тип |  |
|---|---|---|---|
| 1 | `fmt` | string | команда или формат для `string.format` |
| 2 | `...` | any | подстановки |

### Возвращает

Ничего.

### Пример

```lua
sv.cmd("changelevel %s", next_map)
```

С одним аргументом строка уходит как есть — одинокий `%` в нике не превратится в директиву формата.

<Warning>
Длиннее 250 символов — ошибка: обрезанная команда выполнилась бы
как совсем другая.
</Warning>

## sv.set_game_desc {#set_game_desc}

Подменяет текст в колонке "game" сервер-браузера (обычно "Counter-Strike"), рядом с hostname.

```lua
sv.set_game_desc(text)
```

### Аргументы

| # | имя | тип |  |
|---|---|---|---|
| 1 | `text` | string | новый текст; `""` возвращает движковое значение по умолчанию |

### Возвращает

Ничего.

### Пример

```lua
sv.set_game_desc("Public 24/7")
```
