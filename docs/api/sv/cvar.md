---
title: sv — Cvar
description: "Сервер: часы, карта, консоль, переменные движка"
---

# Cvar

## sv.cvar {#cvar}

Возвращает объект существующей переменной движка.

```lua
sv.cvar(name)
```

### Аргументы

| # | имя | тип |  |
|---|---|---|---|
| 1 | `name` | string | имя переменной |

### Возвращает

| тип |  |
|---|---|
| `cvar \| nil` | `nil`, если такой переменной нет |

### Пример

```lua
local ft = sv.cvar("mp_freezetime")
if ft then print(ft:int()) end
```

## sv.cvar_register {#cvar_register}

Заводит свою переменную движка.

```lua
sv.cvar_register(name, default[, flags])
```

### Аргументы

| # | имя | тип |  |
|---|---|---|---|
| 1 | `name` | string | имя переменной |
| 2 | `default` | string | значение по умолчанию, строкой |
| 3 | `flags` | table \| nil | см. [Флаги](#cvar_register-флаги) |

### Возвращает

| тип |  |
|---|---|
| `cvar` | объект переменной |

### Флаги {#cvar_register-флаги}

| поле | тип |  |
|---|---|---|
| `archive` | boolean | значение сохраняется в конфиг |
| `server` | boolean | игроки получают уведомление об изменении |
| `protected` | boolean | значение не отдаётся клиентам, для паролей |

### Пример

```lua
local enabled = sv.cvar_register("greet_enabled", "1", { archive = true })
if enabled:int() ~= 0 then ... end
```

Повторный вызов для существующего имени возвращает объект, не сбрасывая значение, поэтому `lua_reload` cvar'ы не трогает.

## c:int {#int}

Значение переменной как целое.

```lua
c:int()
```

### Возвращает

| тип |  |
|---|---|
| `number` | значение |

## c:float {#float}

Значение переменной как число.

```lua
c:float()
```

### Возвращает

| тип |  |
|---|---|
| `number` | значение |

## c:str {#str}

Значение переменной как строка.

```lua
c:str()
```

### Возвращает

| тип |  |
|---|---|
| `string` | значение |

## c:set {#set}

Записывает значение переменной.

```lua
c:set(value)
```

### Аргументы

| # | имя | тип |  |
|---|---|---|---|
| 1 | `value` | string \| number | новое значение |

### Возвращает

Ничего.

### Пример

```lua
sv.cvar("mp_friendlyfire"):set(1)
```

## c:name {#name}

Имя переменной.

```lua
c:name()
```

### Возвращает

| тип |  |
|---|---|
| `string` | имя |
