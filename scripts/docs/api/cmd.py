# -*- coding: utf-8 -*-
NS = {
    'dir': 'cmd',
    'label': 'cmd',
    'title': 'cmd',
    'brief': 'Команды чата, серверной консоли и rcon.',
    'intro': """
Одна регистрация покрывает чат (`!name`, `/name`), консоль игрока, серверную
консоль и rcon.

| `source` | кто вызывает | `ctx.player` | права |
|---|---|---|---|
| `chat`, `chat_team` | игрок в чате | игрок | проверяются |
| `console` | игрок в своей консоли | игрок | проверяются, как в чате |
| `server` | серверная консоль, rcon | `nil` | не проверяются — это и так высшая власть |

Перепутать `console` и `server` — реальный баг: отладочную команду с
`server` на `console` откроешь любому игроку из его же консоли, а игровую
команду с `console` на `server` — наоборот, запрёшь для rcon.
""",
    'groups': [
        {
            'items': [
                {
                    'name': 'cmd.add',
                    'brief': 'Регистрирует команду.',
                    'sig': 'cmd.add(name, fn[, opts])',
                    'args': [
                        ('name', 'string \\| table', 'имя без префикса; список — алиасы на один обработчик'),
                        ('fn', 'function', 'получает [`ctx`](#ctx)'),
                        ('opts', 'table \\| nil', 'см. [Опции](#опции)'),
                    ],
                    'fields': ('Опции', [
                        ('source', 'string \\| table', '`console`, `server`, `chat`, `chat_team`; список или ничего — везде'),
                        ('layout_alias', 'boolean', 'добавить каждому имени вариант в другой раскладке; по умолчанию `false`'),
                        ('perm', 'string', 'нода прав, проверяется до вызова обработчика'),
                        ('target', 'number', 'номер аргумента с именем игрока → `ctx.target`'),
                        ('immunity', 'boolean', '`false` отключает проверку иммунитета при `target`'),
                    ]),
                    'example': """
cmd.add("heal", function(ctx)
	local p = ctx.player
	if p then p:health(p:health() + 25) end
	ctx.reply("healed")
end)

cmd.add("kick",  fn, { source = "server" })        -- только rcon/серверная консоль
cmd.add("stats", fn, { source = "console" })       -- только консоль самого игрока
cmd.add("plant", fn, { source = "chat_team" })
cmd.add("slay",  fn, { perm = "admin.slay", target = 1 })

-- алиасы: "nomination" и "nominate" — один обработчик
cmd.add({ "nomination", "nominate" }, fn)

-- + раскладочный вариант: "maps" наберут ещё и как "ьфзы"
cmd.add("maps", fn, { layout_alias = true })
""",
                    'extra': """
### ctx

| поле | тип | |
|---|---|---|
| `ctx.player` | player \\| nil | `nil` только для `server` (серверная консоль/rcon) |
| `ctx.args` | table | слова после имени команды |
| `ctx.name` | string | имя команды |
| `ctx.source` | string | `console`, `server`, `chat`, `chat_team` |
| `ctx.reply(text)` | function | ответ туда, откуда пришла команда |
| `ctx.target` | player | игрок из аргумента при `opts.target` |
| `ctx.can(node)` | function | проверить право вызвавшего |

`ctx.reply` из `server` и `console` сама срезает теги цвета чата (`{green}`,
`{default}`, `{team}`) — ни серверная консоль, ни консоль игрока их не
рисуют, один и тот же текст безопасно отдавать в `ctx.reply` независимо от
источника.

Распознанная чат-команда в общий чат не попадает.

С `target` роутер сам находит игрока по слоту, `#userid` или части ника и
отказывает, если у цели иммунитет не ниже.

`layout_alias` перекладывает только латиницу в кириллицу (например,
`"nomination"` → `"тщьштфешщт"` — та же строка, набранная на русской
раскладке). Обратного направления нет: перекладывать кириллическое имя не во
что, латинских имён команд без этого и так достаточно. У имени с чем-то, кроме
`a`-`z`, алиаса просто не появится — без ошибки.

Каждое имя (и алиас, и раскладочный вариант) — свой пункт в
[`cmd.list()`](list.md) и своя запись в [`cmd.remove`](remove.md): сняв
`"nomination"`, `"тщьштфешщт"` не трогаешь, снимать нужно каждое имя, которое
регистрировал.
""",
                    'notes': [
                        ('note', '`console` — это игрок печатает команду в своей клиентской консоли,\nа не в чате: `ctx.player` — он сам, `perm` и `immunity` проверяются\nровно как в чате. `server` — серверная консоль и rcon: `ctx.player`\nвсегда `nil`, прав нет вообще — это и так высшая власть на машине.\nПерепутать их — реальная дыра или реальный баг, смотри таблицу выше.'),
                        ('warning', 'Имя команды регистрируется в движке навсегда. `lua_reload`\nменяет только Lua-обработчик: если плагин не объявил команду\nзаново, она становится пустышкой.'),
                    ],
                    'see': [('players.find', '../players/find.md'), ('access', '../access/index.md')],
                },
                {
                    'name': 'cmd.remove',
                    'brief': 'Снимает команду.',
                    'sig': 'cmd.remove(name)',
                    'args': [('name', 'string', 'имя команды')],
                    'returns': [('boolean', '`true`, если команда была')],
                },
                {
                    'name': 'cmd.list',
                    'brief': 'Возвращает имена зарегистрированных команд.',
                    'sig': 'cmd.list()',
                    'args': [],
                    'returns': [('table', 'отсортированный массив строк')],
                },
            ],
        },
    ],
}
