# -*- coding: utf-8 -*-

RANGE_NOTE = ('warning', 'Значение вне диапазона — ошибка Lua сразу в момент вызова `msg.byte`/\n`msg.short`/..., до `msg.send`. Так проще найти, какое именно поле не влезло.')


def field(name, arg, range_, sdk, extra=None):
    return {
        'name': 'msg.%s' % name,
        'brief': '%s — %s (`%s` в SDK).' % (arg, range_, sdk),
        'sig': 'msg.%s(%s)' % (name, arg),
        'args': [(arg, 'number \\| string', range_)],
        'returns': [('table', 'значение для `msg.send` — передавай как есть, не пересобирай руками')],
        'extra': extra,
        'notes': [RANGE_NOTE] if name not in ('string', 'entity') else None,
    }


NS = {
    'dir': 'msg',
    'label': 'msg',
    'title': 'msg',
    'brief': 'Отправка сырых сетевых сообщений движка — своих и стоковых.',
    'intro': """
`msg` отправляет сетевое user-сообщение напрямую — то же самое, что
`MESSAGE_BEGIN`/`WRITE_*`/`MESSAGE_END` в SDK, но одним вызовом. Подходит и
для стоковых сообщений игры (`TextMsg`, `ScreenFade`, `Damage` и другие,
которые уже умеет читать клиент), и для полностью своих — под клиентский мод,
который сам их разбирает.

```lua
msg.send("one", "TextMsg", e.player,
  msg.byte(4), msg.string("#Game_dead"))
```

## Почему не begin/write/end

В SDK `MESSAGE_BEGIN` открывает сообщение, `WRITE_*` пишут в него поля,
`MESSAGE_END` закрывает. Если дать Lua самому вызывать эти шаги по очереди,
любая ошибка между `begin` и `end` — не то поле, `nil`, что угодно —
оставит сообщение недописанным. Это не падение одного плагина: битый или
недооткрытый пакет уходит в общий сетевой поток движка и задевает всех
на сервере.

Поэтому `msg.send` — один вызов. Все аргументы и все поля собираются и
проверяются в Lua заранее; `MESSAGE_BEGIN` вызывается только тогда, когда
упасть уже физически нечему.

Приём чужих сообщений — не здесь, а в [`hook.add("msg:Name", ...)`](../hook/namespace.md#add-msg).
""",
    'groups': [
        {
            'title': 'Отправка',
            'slug': 'send',
            'items': [
                {
                    'name': 'msg.send',
                    'brief': 'Собирает и отправляет одно сетевое сообщение.',
                    'sig': 'msg.send(dest, name, target, ...)',
                    'args': [
                        ('dest', 'string', 'куда слать — см. ниже'),
                        ('name', 'string', 'имя сообщения'),
                        ('target', 'player \\| entity \\| table \\| nil', 'получатель или точка, зависит от `dest`'),
                        ('...', 'поля из `msg.byte`/`msg.string`/...', 'данные сообщения, по порядку в проводе'),
                    ],
                    'returns': [('boolean', '`true` при успехе; при неверных аргументах — ошибка Lua, сообщение не уходит вообще (ни частично, ни целиком)')],
                    'example': """
hook.add("player:death", "myplugin.fade", function(e)
  msg.send("one", "ScreenFade", e.player,
    msg.short(1000),   -- duration
    msg.short(0),      -- hold
    msg.short(0),      -- flags
    msg.byte(0), msg.byte(0), msg.byte(0), msg.byte(200))
end)
""",
                    'extra': """
## dest

| Значение | Куда уходит | `target` |
|---|---|---|
| `"one"` | Одному игроку, надёжно | игрок — обязателен |
| `"one_unreliable"` | Одному игроку, ненадёжно (может потеряться) | игрок — обязателен |
| `"all"` | Всем подключённым | не используется, передавай `nil` |
| `"pvs"` | Всем, кто видит точку (PVS) | игрок, сущность или `{x, y, z}` — обязателен |
| `"pas"` | Всем, кто слышит точку (PAS) | игрок, сущность или `{x, y, z}` — обязателен |

Для `"pvs"`/`"pas"` игрок или сущность в `target` — просто источник точки:
берётся `origin` их `edict`, дальше это обычная рассылка по видимости/слышимости,
не «этому конкретному игроку».

## name

Имя сообщения ищется через движок (`RegUserMsg`), не через жёстко прошитый
список:

- Если сообщение с таким именем уже зарегистрировано игровой библиотекой
  (`TextMsg`, `ScreenFade`, `Damage`, `BarTime`, ...) — вернётся тот же id,
  и клиент обработает его штатно.
- Если имя никто ещё не занял — движок зарегистрирует новое сообщение под
  этим именем. Это годится только для клиента, который сам умеет его читать
  (свой клиентский мод); стоковый `hl.dll`/движок просто проигнорирует
  неизвестное имя.

Id резолвится один раз и кешируется — второй `msg.send` с тем же `name`
ничего не переспрашивает у движка.

## Поля

Порядок аргументов после `target` — это порядок байт в проводе. Он должен
совпадать с тем, что ждёт получатель (для стоковых сообщений — с тем, что
разбирает клиент; для своих — с тем, что разбирает твой клиентский код).
Типы полей — [`msg.byte` и соседние](fields.md).
""",
                    'notes': [('warning', 'Если `dest` требует получателя/точку (`one`, `one_unreliable`, `pvs`, `pas`),\nа `target` не подходит — ошибка Lua до всякой отправки. Само сообщение при\nэтом никогда не уходит наполовину: либо `MESSAGE_BEGIN`/`MESSAGE_END`\nотработали целиком, либо не начинались вовсе.')],
                },
            ],
        },
        {
            'title': 'Поля сообщения',
            'slug': 'fields',
            'note': """
Каждая функция описывает одно поле в порядке провода — сама ничего не
отправляет, только проверяет диапазон и возвращает значение, которое
`msg.send` пишет по очереди.

```lua
msg.send("one", "TextMsg", e.player,
  msg.byte(4), msg.string("hello"))
```
""",
            'items': [
                field('byte', 'n', '`0..255`', 'WRITE_BYTE'),
                field('char', 'n', '`-128..127`', 'WRITE_CHAR'),
                field('short', 'n', '`-32768..32767`', 'WRITE_SHORT'),
                field('long', 'n', 'любое 32-битное целое', 'WRITE_LONG'),
                field('angle', 'f', 'число, градусы', 'WRITE_ANGLE'),
                field('coord', 'f', 'число, игровые единицы', 'WRITE_COORD'),
                field('string', 's', 'до 191 байта', 'WRITE_STRING',
                      extra='Проходит через ту же кодировку, что и `p:hud()`/`p:say()`: `cslua_cp1251` переключает UTF-8 → CP1251 для старых клиентов, по умолчанию выключено.'),
                field('entity', 'v', 'игрок или объект `ents`, индекс `>= 1`', 'WRITE_ENTITY',
                      extra='Принимает и игрока (`e.player`), и сущность из `ents` — что угодно с полем `id` (игрок) или `index` (сущность). `0` (мир) не годится.'),
            ],
        },
    ],
}
