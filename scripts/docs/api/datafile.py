# -*- coding: utf-8 -*-
NS = {
    'dir': 'datafile',
    'label': 'datafile',
    'title': 'datafile',
    'brief': 'Чтение и запись читаемых `.lua`-файлов, которые правят руками.',
    'intro': """
```lua
local files = require("datafile").at(plugin.data_dir())

local cfg = files.load("settings", { rounds = 3 })
cfg.rounds = cfg.rounds + 1
files.save("settings", cfg)
```

Формат — обычный Lua, который возвращает таблицу: ни парсера, ни второго
синтаксиса конфига. Подходит для того, что редактирует человек: список
админов, набор префиксов, сток магазина. Им же ядро хранит `data/users.lua`.

**Запись атомарна.** Новое содержимое пишется в `.tmp`, старый файл переезжает
в `.bak`, и только потом `.tmp` встаёт на место. **Вывод детерминированный:**
ключи сортируются, две записи одних и тех же данных дают побайтово одинаковый
файл — правки руками переживают запись, комментарии нет.

## Что выбрать

| | `datafile` | [`db`](../db/index.md) | [`mysql`](../mysql/index.md) |
|---|---|---|---|
| конфиг, список админов, префиксы | да | нет | нет |
| правится руками, ложится в git диффом | да | нет | нет |
| статистика, балансы, свои баны | нет | да | да |
| выборки, сортировки, топ-10 | нет | да | да |
| писать надо часто, тысячи строк | нет | да | да |
| база общая или удалённая | нет | нет | да |

`datafile` переписывает файл целиком на каждый `save` — это для десятков
значений, не для записи на каждый фраг. Функции и userdata не сохраняются:
это ошибка, а не тихая потеря.
""",
    'groups': [
        {
            'items': [
                {
                    'name': 'datafile.at',
                    'brief': 'Привязывает модуль к своему каталогу.',
                    'sig': 'datafile.at(dir)',
                    'args': [('dir', 'string', 'существующий каталог')],
                    'returns': [('table', 'тот же набор функций, но в этом каталоге')],
                    'example': 'local files = require("datafile").at(plugin.data_dir())',
                    'extra': 'Без `at()` функции работают в общем `addons/lua/data/`, где лежит `users.lua` ядра.',
                },
                {
                    'name': 'datafile.load',
                    'brief': 'Читает таблицу из `<dir>/<name>.lua`.',
                    'sig': 'datafile.load(name[, fallback])',
                    'args': [('name', 'string', 'имя файла без расширения'),
                             ('fallback', 'any', 'что вернуть, если файла нет')],
                    'returns': [('table', 'содержимое файла'),
                                ('string', 'причина, если файл битый')],
                    'extra': 'Отсутствие файла — не ошибка: на свежем сервере данных ещё нет. Битый файл — ошибка, и она возвращается вторым значением.',
                    'notes': [('note', 'Файл выполняется в пустом окружении: ни `io`, ни `os`, ни `print`.\nВсё, кроме возвращённой таблицы, — ошибка.')],
                },
                {
                    'name': 'datafile.save',
                    'brief': 'Пишет таблицу в `<dir>/<name>.lua`.',
                    'sig': 'datafile.save(name, t[, header])',
                    'args': [('name', 'string', 'имя файла без расширения'),
                             ('t', 'table', 'что записать'),
                             ('header', 'string \\| nil', 'комментарий первой строкой')],
                    'returns': [('boolean', '`true` при успехе'),
                                ('string', 'причина при ошибке')],
                },
                {
                    'name': 'datafile.serialize',
                    'brief': 'Превращает таблицу в текст, без записи.',
                    'sig': 'datafile.serialize(t)',
                    'args': [('t', 'table', 'что сериализовать')],
                    'returns': [('string', 'Lua-литерал')],
                },
                {
                    'name': 'datafile.dir',
                    'brief': 'Каталог, к которому привязан модуль.',
                    'sig': 'datafile.dir()',
                    'args': [],
                    'returns': [('string', 'абсолютный путь')],
                },
            ],
        },
    ],
}
